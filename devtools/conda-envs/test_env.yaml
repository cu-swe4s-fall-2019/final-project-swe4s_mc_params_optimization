name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - tqdm
  - scipy
  - numpy
  - pandas
  - pyyaml
  - matplotlib

    # Pip-only installs
  #- pip:
  #  - codecov

